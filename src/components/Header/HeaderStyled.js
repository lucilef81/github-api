import styled from 'styled-components';

const HeaderStyled = styled.div`
  .header-icon {
    display: flex;
    align-items: center;

    h1 {
      margin-top: 0;
      font-size: 1.6rem;
    }
  }
`;

export default HeaderStyled;
